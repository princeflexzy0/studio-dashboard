# Studio Dashboard Environment Variables

# App Configuration
NEXT_PUBLIC_APP_NAME=Studio Dashboard
NEXT_PUBLIC_APP_URL=http://localhost:3000

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Authentication (Mock for demo)
# In production, use secure authentication service
AUTH_SECRET=your-secret-key-here

# Performance Tips:
# - Use environment variables for sensitive data
# - Never commit .env files to version control
# - Keep API keys secure
# - Use NEXT_PUBLIC_ prefix for client-side variables only

# Database (when you add one)
# DATABASE_URL=postgresql://user:password@localhost:5432/studio

# Email Service (when you add one)
# EMAIL_SERVICE_API_KEY=your-email-api-key

# File Upload Service (when you add one)
# UPLOAD_MAX_SIZE=10485760
# UPLOAD_ALLOWED_TYPES=image/jpeg,image/png,video/mp4
